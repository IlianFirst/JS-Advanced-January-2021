describe('Sum numbers', () =>{
    it('sums single nunber', () => {
        expect(sum([1])).to.equal(1);
    });

    it('sums multiple nunber', () => {
        expect(sum([1,1])).to.equal(2);
    });

    it('sums different nunber', () => {
        expect(sum([2,3,4])).to.equal(9);
    });
});
