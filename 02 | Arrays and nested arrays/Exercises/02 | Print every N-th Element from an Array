function printEveryNelement(arr, step){
    let result = [];
    for (let index = 0; index < arr.length; index++){
        if (index % step === 0){
            result.push(arr[index]);
        }
    }
    return result;
}
